# AI Sana Tasks

MVP платформы для подготовки, рейтинга и открытого выбора бизнес-задач. Представитель бизнеса превращает слабое описание в подтверждённую карточку, повышает её рейтинг полноты и публикует в каталоге. Студенческие команды самостоятельно выбирают задачи и отправляют предложения, а бизнес вручную принимает или отклоняет их.

> Организационный риск: в кейсе указана команда из 3–5 участников. Нас двое, поэтому до старта нужно подтвердить у организаторов допустимость такого состава или найти третьего участника.

## Статус

Сейчас готов технический каркас без бизнес-логики:

- Next.js App Router и TypeScript;
- Tailwind CSS;
- общие Zod-контракты;
- границы frontend, backend и shared-кода;
- шаблон переменных окружения;
- health-check API;
- конфигурация рейтинга и базовый тест;
- страницы-заглушки обязательного сценария.

## Быстрый старт

Требования:

- Node.js 22;
- pnpm 11 через Corepack;
- Git.

```powershell
git clone https://github.com/BAITC-Hacks/hack-cb606f99-zsa.git
cd hack-cb606f99-zsa
corepack enable
pnpm install --frozen-lockfile
Copy-Item .env.example .env.local
pnpm dev
```

Для macOS/Linux вместо `Copy-Item`:

```bash
cp .env.example .env.local
```

Приложение: [http://localhost:3000](http://localhost:3000)

Проверка backend: [http://localhost:3000/api/health](http://localhost:3000/api/health)

## Команды

| Команда | Назначение |
|---|---|
| `pnpm dev` | Локальная разработка |
| `pnpm lint` | ESLint |
| `pnpm typecheck` | Проверка TypeScript |
| `pnpm test` | Unit-тесты |
| `pnpm build` | Production-сборка |
| `pnpm check` | Полная проверка перед merge |

## Переменные окружения

Скопируйте `.env.example` в `.env.local`. Настоящие `.env*` игнорируются Git, а `.env.example` хранит только безопасный шаблон.

| Переменная | Обязательна | Назначение |
|---|---:|---|
| `AI_PROVIDER` | Да | `mock` для локальной заглушки или `openai` для API |
| `OPENAI_API_KEY` | Для `openai` | Секретный ключ; никогда не добавлять `NEXT_PUBLIC_` |
| `OPENAI_MODEL` | Для `openai` | Доступная команде модель |
| `DATA_FILE_PATH` | Нет | Путь к локальному JSON-хранилищу |
| `NEXT_PUBLIC_APP_NAME` | Нет | Публичное название приложения |

## Технологии

- Next.js 16 App Router;
- React 19;
- TypeScript;
- Tailwind CSS 4;
- Zod 4;
- OpenAI TypeScript SDK;
- Vitest;
- JSON-файл за интерфейсом репозитория для локального MVP.

Отдельная база данных, Docker, авторизация и production-инфраструктура сознательно не входят в каркас: они не нужны для обязательной пятичасовой демонстрации. Слой репозитория позволит заменить JSON на SQLite или PostgreSQL позже.

## Структура проекта

```text
src/
  app/
    api/                  Route Handlers, вход в backend
    business/             Экраны представителя бизнеса
    catalog/              Общий каталог
    tasks/                Карточка задачи для команды
  components/             UI и feature-компоненты
  server/
    ai/                   Промпты и AI-провайдеры
    domain/               Рейтинг и бизнес-правила
    repositories/         Работа с JSON-хранилищем
    services/             Сценарии приложения
    env.ts                Серверные переменные окружения
  shared/
    contracts.ts          Общие Zod-схемы и TypeScript-типы
    scoring.ts            Единая конфигурация рейтинга
data/                     Локальные данные, кроме .gitkeep не коммитятся
```

Правила границ:

- frontend работает в `src/app` и `src/components`;
- backend работает в `src/server` и `src/app/api`;
- frontend не импортирует `src/server`;
- оба используют схемы из `src/shared`;
- изменения общих контрактов сначала согласуются, затем меняются одним небольшим коммитом;
- API-ключи доступны только серверному коду.

## Обязательный пользовательский сценарий

1. Бизнес вводит слабое описание потребности или проблемы.
2. Система определяет недостающие сведения и задаёт минимум три уместных вопроса.
3. Из описания и ответов формируется редактируемая карточка.
4. Бизнес вручную подтверждает поля.
5. Сервер рассчитывает рейтинг от 0 до 100 и объясняет начисление.
6. Задача публикуется в общем каталоге независимо от рейтинга.
7. Студенческая команда отправляет идею, план, срок и ссылку.
8. Бизнес вручную принимает или отклоняет предложение.

AI не публикует задачу, не добавляет неизвестные пользователю факты и не выбирает команду.

## Поля карточки

- название;
- исходное описание;
- отрасль и тема;
- контекст и потребность;
- доступные данные и материалы;
- ожидаемый результат;
- критерии успеха;
- ограничения;
- целевые пользователи;
- контакт бизнеса;
- формат взаимодействия.

Актуальная машинно-проверяемая схема находится в `src/shared/contracts.ts`.

## Рейтинг готовности

| Показатель | Баллы |
|---|---:|
| Контекст и потребность | 20 |
| Данные и материалы | 20 |
| Ожидаемый результат | 15 |
| Критерии успеха | 15 |
| Ограничения | 10 |
| Пользователи | 10 |
| Связь с бизнесом | 10 |
| **Всего** | **100** |

Баллы начисляются сервером только за заполненные и подтверждённые человеком поля. AI не рассчитывает итоговый рейтинг.

Уровни готовности:

- 0–39 — черновик;
- 40–69 — рабочая;
- 70–89 — готовая;
- 90–100 — приоритетная.

Низкий рейтинг не скрывает задачу и не запрещает отклик. Рейтинг влияет на позицию в каталоге и пересчитывается после подтверждённых изменений.

## Планируемый API

| Метод | Маршрут | Назначение | Статус |
|---|---|---|---|
| `GET` | `/api/health` | Проверка приложения | Готово |
| `POST` | `/api/ai/questions` | Анализ черновика и минимум 3 вопроса | План |
| `POST` | `/api/ai/card` | Формирование карточки из ответов | План |
| `GET` | `/api/tasks` | Каталог, сортировка и фильтры | План |
| `POST` | `/api/tasks` | Создание черновика | План |
| `PATCH` | `/api/tasks/:id` | Редактирование и подтверждение полей | План |
| `POST` | `/api/tasks/:id/publish` | Публикация карточки | План |
| `POST` | `/api/tasks/:id/proposals` | Отклик команды | План |
| `PATCH` | `/api/proposals/:id/status` | Ручное решение бизнеса | План |

Формат ошибки API:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Human-readable description",
    "details": []
  }
}
```

## AI-контракт

Первая функция анализирует черновик и возвращает:

- минимум три уточняющих вопроса;
- поле карточки, к которому относится вопрос;
- причину вопроса;
- список недостающих полей.

Вторая функция получает исходный текст и ответы и возвращает только структурированные поля карточки. Ответ всегда проверяется Zod-схемой. При недоступности API используется `mock`-провайдер с тем же форматом ответа.

## Распределение работы

### Backend

- `src/server`, `src/app/api` и `data`;
- JSON-репозиторий и seed-данные;
- формула рейтинга;
- AI-промпт, Structured Output и fallback;
- API задач, публикации и предложений;
- валидация, ошибки и unit-тесты.

### Frontend

- `src/app`, кроме `src/app/api`;
- `src/components`;
- мастер создания задачи;
- карточка и визуализация рейтинга;
- каталог и фильтры;
- форма предложения и решение бизнеса;
- loading, empty и error states.

### Совместно

- `src/shared`;
- интеграция полного сценария;
- README и демонстрация.

## Git-процесс для двух человек

1. Не работать напрямую в одном длинном branch.
2. Использовать короткие ветки `backend/<задача>` и `frontend/<задача>`.
3. Перед началом новой задачи обновляться от `main`.
4. Делать небольшие коммиты в формате Conventional Commits:
   - `feat: add task draft endpoint`;
   - `fix: recalculate readiness after confirmation`;
   - `docs: update demo flow`.
5. Не коммитить `.env.local`, ключи и содержимое `data/*.json`.
6. Перед merge запускать `pnpm check`.
7. Интегрировать изменения регулярно, не оставлять объединение на последние 30 минут.

## Не входит в MVP

- полноценная регистрация и восстановление пароля;
- сложная ролевая модель;
- чат, уведомления и календарь;
- файловое хранилище;
- собственная ML-модель и векторная база;
- автоматическое назначение команды;
- production-инфраструктура;
- полноценный проектный трекер.

Для демонстрации роли можно переключать явным контролом «Бизнес / Команда» без авторизации.

## Обязательная демонстрация

За пять минут нужно в работающем приложении:

1. ввести слабое описание;
2. ответить на AI-вопросы;
3. показать заполненную карточку и рост рейтинга;
4. опубликовать задачу;
5. открыть её от лица команды;
6. отправить предложение;
7. вернуться к роли бизнеса;
8. принять или отклонить предложение.

Все переходы должны работать в MVP, а не на слайдах.
